# ideaForge Technology Limited / IDEAFORGE
Generated 2026-05-14 | Fundamental rank `#10` | Coverage mode `full 10-stock working universe`

**symbol:** IDEAFORGE  
**company:** ideaForge Technology Limited  
**technical_data_mode:** `api_fallback`  
**cmp:** `771.10`  
**3pct_floor:** `748.97`  
**practical_stop_zone:** `744.00-752.00`

**weekly_note:** API evidence: `stock-1wk.json` and `indicators-1wk.json`; all required EMA values were present, so no null-indicator fallback was needed. Weekly market structure remains bullish because price is still above the full EMA stack after the breakout week, and the chart pattern is a breakout-and-pullback rather than a breakdown; volume on the breakout week was decisive, while the current pullback is lighter. Weekly demand sits around `720.00-682.60`, and weekly supply is the `849.35` high.

**daily_note:** API evidence: `stock-1d.json` and `indicators-1d.json`; all required EMA values were present, so no null-indicator fallback was needed. Daily structure is a V-shaped reversal off the `720.00-732.20` flush, with the close back above `771.10` and above the `754.37` 10EMA; MACD and RSI stay constructive, but the candle is still repairing a sharp selloff rather than trending cleanly. Daily demand is `754.37` then `732.20`, and daily supply is `798.50-832.00`.

**60_note:** API evidence: `stock-60m.json` and `indicators-60m.json`; all required EMA values were present, so no null-indicator fallback was needed. The 60m structure is corrective because price is below the full EMA stack, so the intraday trend has not reclaimed control; the pattern is a post-flush bounce with active volume, not a clean continuation base. The nearest demand shelf is `767.40`, while overhead supply is the `824.64` EMA10 area and the prior `804.60` intraday pivot.

**30_note:** API evidence: `stock-30m.json` and `indicators-30m.json`; all required EMA values were present, so no null-indicator fallback was needed. The 30m execution frame is the key weakness: price is still under the `820.39-836.50` EMA supply block, and the sequence of lower highs from `784.10` into `771.10` shows the rebound has not yet reclaimed control. The `767.40` swing low is the near-term demand line, and volume expanded on the drop, which means the stop path needs to hold quickly if support is tested again.

**15_note:** API evidence: `stock-15m.json` and `indicators-15m.json`; all required EMA values were present, so no null-indicator fallback was needed. The 15m micro structure is the weakest of the set because price is only barely above the `770.41` 200EMA while the 10/20/50/100 EMAs are overhead, and MACD momentum has rolled negative. That leaves `767.40` as the immediate line in the sand, with active sell-volume showing this is a live but fragile base rather than a clean micro uptrend.

**support_inventory**

| timeframe | support type | price level | distance from CMP |
|---|---|---:|---:|
| 15m | micro demand shelf | `767.40` | `-0.5%` |
| 30m | swing low / demand shelf | `767.40` | `-0.5%` |
| 60m | pullback low / demand shelf | `767.40` | `-0.5%` |
| 1d | EMA10 support | `754.37` | `-2.2%` |
| 1d | reversal low / demand | `732.20` | `-5.0%` |
| 1wk | weekly floor | `720.00` | `-6.6%` |

**resistance_inventory**

| timeframe | resistance type | price level | distance from CMP |
|---|---|---:|---:|
| 15m | local supply | `784.10` | `+1.7%` |
| 30m | intraday pivot high | `797.25` | `+3.4%` |
| 60m | intraday pivot high | `804.60` | `+4.3%` |
| 1d | swing high supply | `798.50` | `+3.6%` |
| 1d | upper supply | `832.00` | `+7.9%` |
| 1wk | breakout high supply | `849.35` | `+10.1%` |

**entry_zone:** `767.40-776.00` | Tactical only if the intraday demand shelf holds; above `776` the chart starts working back toward the `784` resistance pivot.  
**stop_zone:** `744.00-752.00` | This frames the practical `3%` stop band while leaving room for ordinary noise beneath the daily `754.37` support.  
**first_trouble_area:** `784.10-799.00` | First overhead supply from the 15m/30m pivots and the daily swing-high zone.  
**swing_target:** `832.00-849.35` | Higher-timeframe continuation target if the daily rebound matures and the weekly breakout resumes.

**technical_verdict:** Conditional long, but fragile  
**stop_survivability_label:** fragile  
**primary_failure_risk:** Loss of `767.40` and failure to reclaim the 30m/60m EMA supply can cascade into `754.37` and then the `732.20` daily flush low before a fresh base forms.  
**ranking_reason:** Fundamental rank `#10` and `Mixed` sponsorship stay bottom-tier because the turnaround and order-book build are real, but negative full-year PAT, negative ROCE, and negative operating cash flow still dominate; technically the weekly and daily trends are constructive, yet the lower-timeframe EMA stack is overhead and the stop path is not cleanly defended.  
**summary_rationale:** Weekly and daily structure remain intact, but the 15m-60m frame is still corrective and the latest pullback came with active volume. That means the `2%-4%` stop band is only partially buffered by nearby demand, so `767.40` must hold and `784.10` must be reclaimed before the setup looks reliably defendable.
